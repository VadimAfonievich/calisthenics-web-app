import{api}from'./client';import{requireEntityID}from'./entityIds'
export type CoachRole='coach'|'admin'|'super_admin';export type Dashboard={lessons:number;lessons_published:number;exercises:number;workouts:number;programs:number;skills:number;media:number};export type ContentItem={id:string;name:string;slug?:string;status:'draft'|'published'|'archived';difficulty:string;owner_user_id?:string;updated_at:string};export type LessonBlock={type:'heading'|'text'|'image'|'video'|'tip'|'warning'|'checklist'|'divider';text?:string;media_id?:string;items?:string[]};export type LessonInput={category_id:string;title:string;slug:string;short_description:string;content:string;difficulty:string;duration_minutes:number;cover_media_id?:string;blocks:LessonBlock[]};export type MediaAsset={id:string;type:'image'|'video';url:string;thumbnail_url?:string;original_filename:string;mime_type:string;size_bytes:number;references:number;status:string}
export const coachMe=(t:string)=>api<{role:CoachRole}>('/coach/me',{},t),dashboard=(t:string)=>api<Dashboard>('/coach/dashboard',{},t),analytics=(t:string)=>api<Record<string,unknown>>('/coach/analytics',{},t),content=(t:string,kind:string,search='',status='')=>api<{items:ContentItem[]}>(`/coach/${kind}?${new URLSearchParams({search,status})}`,{},t),createLesson=(t:string,x:LessonInput)=>api<{id:string}>('/coach/lessons',{method:'POST',body:JSON.stringify(x)},t),updateLesson=(t:string,id:string,x:LessonInput)=>api<{id:string}>(`/coach/lessons/${requireEntityID(id,'Lesson')}`,{method:'PUT',body:JSON.stringify(x)},t),action=(t:string,kind:string,id:string,name:'publish'|'unpublish'|'archive'|'duplicate')=>api<{id:string;status?:string}>(`/coach/${kind}/${requireEntityID(id,'Content')}/${name}`,{method:'POST'},t),media=(t:string)=>api<{media:MediaAsset[]}>('/coach/media',{},t),addExternalMedia=(t:string,x:{type:'image'|'video';url:string;original_filename:string;mime_type:string;size_bytes:number})=>api<{media:MediaAsset}>('/coach/media',{method:'POST',body:JSON.stringify(x)},t),deleteMedia=(t:string,id:string)=>api<void>(`/coach/media/${requireEntityID(id,'Media')}`,{method:'DELETE'},t)
