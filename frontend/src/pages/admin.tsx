import{useState}from'react';import{APIError,api}from'../api/client';import{useSessionStore}from'../store/session'
const templates={lesson:{category_id:'',title:'',slug:'',short_description:'',content:'',difficulty:'beginner',duration_minutes:5,sort_order:0,published:false},exercise:{name:'',slug:'',description:'',instructions:'',common_mistakes:'',difficulty:'beginner',muscle_groups:[''],equipment:[]},program:{name:'',slug:'',description:'',difficulty:'beginner',duration_weeks:4,published:false},workout:{program_id:'',day_number:1,title:'',description:'',estimated_minutes:20,sort_order:0}}
export function AdminPage(){const t=useSessionStore(s=>s.accessToken),[kind,setKind]=useState<keyof typeof templates>('lesson'),[body,setBody]=useState(JSON.stringify(templates.lesson,null,2)),[result,setResult]=useState('');const choose=(x:keyof typeof templates)=>{setKind(x);setBody(JSON.stringify(templates[x],null,2));setResult('')};const submit=async()=>{try{const data=JSON.parse(body);const r=await api<{id:string}>(`/admin/${kind==='lesson'?'lessons':kind==='exercise'?'exercises':kind==='program'?'programs':'workouts'}`,{method:'POST',body:JSON.stringify(data)},t);setResult(`Создано: ${r.id}`)}catch(e){setResult(e instanceof APIError?e.message:'Проверьте JSON и поля формы.')}};if(!t)return <div className="empty-state"><h2>Администрирование доступно после входа</h2></div>;return <div className="stack"><section className="hero-card"><p className="eyebrow">ADMIN CMS</p><h2>Управление контентом</h2><span>Доступ предоставляется через роль администратора.</span></section><section className="card"><div className="admin-tabs">{Object.keys(templates).map(x=><button key={x} className={kind===x?'active':''} onClick={()=>choose(x as keyof typeof templates)}>{x}</button>)}</div><p>Создать {kind}. Поля можно заполнить или вставить готовый JSON.</p><textarea value={body} onChange={e=>setBody(e.target.value)} rows={16}/><button className="primary-button" onClick={submit}>Создать</button>{result&&<p className="notice">{result}</p>}</section><section className="card"><h3>Редактирование и публикация</h3><p>Используйте тот же JSON с API: PUT/DELETE /api/v1/admin/{kind}s/:id; для уроков и программ — POST /publish с полем published: true.</p></section></div>}
