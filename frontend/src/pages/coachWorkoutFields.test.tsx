// @vitest-environment jsdom
import {cleanup,render,screen} from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import {afterEach,describe,expect,it,vi} from 'vitest'
import {WorkoutFields} from './coachAuthoring'
afterEach(cleanup)
describe('standalone Coach workout fields',()=>{it('has warmup control and no program coupling fields',()=>{render(<WorkoutFields value={{title:'Standalone',description:'Plan',difficulty:'beginner',category:'strength',estimated_minutes:20,warmup_enabled:true,exercises:[]}} change={vi.fn()} opts={{categories:[],exercises:[],programs:[],program_levels:[],workouts:[],warmups:[],skills:[],media:[]}}/>);expect(screen.getByLabelText('Разминка перед тренировкой')).toBeTruthy();expect(screen.getByText('Выберите разминку')).toBeTruthy();expect(screen.queryByText('Этап программы')).toBeNull();expect(screen.queryByText('День программы')).toBeNull();expect(screen.queryByText('Выберите программу')).toBeNull()})})
it('selects a published warmup and clears it when warmup is disabled',async()=>{const change=vi.fn();const value={title:'Main',description:'Plan',difficulty:'beginner',category:'strength',estimated_minutes:20,warmup_enabled:true,exercises:[]};const opts={categories:[],exercises:[],programs:[],program_levels:[],workouts:[],warmups:[{id:'59000000-0000-0000-0000-000000000001',name:'Полная разминка',status:'published' as const}],skills:[],media:[]};const {rerender}=render(<WorkoutFields value={value} change={change} opts={opts}/>);await userEvent.selectOptions(screen.getByRole('combobox'),'59000000-0000-0000-0000-000000000001');expect(change).toHaveBeenLastCalledWith(expect.objectContaining({warmup_workout_id:'59000000-0000-0000-0000-000000000001'}));rerender(<WorkoutFields value={{...value,warmup_workout_id:'59000000-0000-0000-0000-000000000001'}} change={change} opts={opts}/>);await userEvent.click(screen.getByLabelText('Разминка перед тренировкой'));expect(change).toHaveBeenLastCalledWith(expect.objectContaining({warmup_enabled:false,warmup_workout_id:undefined}))})
